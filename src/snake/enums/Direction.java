package snake.enums;

/**
 * Enum {@code Direction} określa możliwe kierunki ruchu węża w grze.
 */
public enum Direction {
    /** Ruch w górę. */
    UP,

    /** Ruch w dół. */
    DOWN,

    /** Ruch w lewo. */
    LEFT,

    /** Ruch w prawo. */
    RIGHT
}
