package snake.enums;

/**
 * Enum `SnakeType` definiuje typ węża w grze:
 * gracz lub jeden z dwóch węży AI.
 */
public enum SnakeType {
    /** Wąż sterowany przez gracza. */
    PLAYER,

    /** Pierwszy wąż sterowany przez AI. */
    AI1,

    /** Drugi wąż sterowany przez AI. */
    AI2
}